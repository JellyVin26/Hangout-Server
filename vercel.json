{
  "version": 2,
  "buildCommand": "node scripts/prepare-vercel.js && npx prisma db push --schema prisma/schema.postgres.prisma && npx tsx prisma/seed.ts && npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "crons": [
    { "path": "/notifications/reminders/run?secret=cron", "schedule": "* * * * *" }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
